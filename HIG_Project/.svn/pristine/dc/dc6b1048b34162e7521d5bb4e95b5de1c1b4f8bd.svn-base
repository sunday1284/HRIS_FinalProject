<!-- 
 * == 개정이력(Modification Information) ==
 *   
 *   수정일      			수정자           수정내용
 *  ============   	============== =======================
 *  2025. 4. 14.     	정태우            최초 생성
 *
-->
<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>


<style>
    /* 메시지를 이쁘게 꾸미는 CSS */
    .qr-alert-message {
        text-align: center;
        font-size: 2rem; /* 글씨 크기 */
        font-weight: bold;
        color: #856404; /* 어두운 노란색으로 부드럽게 */
        background-color: #fff3cd; /* 연한 노란색 배경 */
        border: 2px solid #ffeeba; /* 배경에 맞는 테두리 */
        padding: 20px; /* 여백 */
        border-radius: 10px; /* 모서리 둥글게 */
        max-width: 600px; /* 최대 너비 설정 */
        margin: 50px auto; /* 가운데 정렬 */
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1); /* 그림자 효과 */
    }
</style>

<!-- 메시지 부분 -->
<h1 class="qr-alert-message">${qrAlertMessage}</h1>

